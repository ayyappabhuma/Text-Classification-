Submissions contain
1.Report file (writeup on the project)
2.text_classification.r  (R code which comprises cleaning and visualizations of data)
3.Text_Classification.ipynb  (ipython notebook comprising model building)

First run the R code which writes a .csv file in working directory(which I have already included) that is taken as input for python code.